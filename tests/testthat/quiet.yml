sources:
  quiet.R

targets:
  noisy_message:
    rule: noisy_message
  noisy_cat:
    rule: noisy_cat
  noisy_warning:
    rule: noisy_warning
  noisy_error:
    rule: noisy_error

  quiet_message:
    rule: noisy_message
    quiet: true
  quiet_cat:
    rule: noisy_cat
    quiet: true
  quiet_warning:
    rule: noisy_warning
    quiet: true
  quiet_error:
    rule: noisy_error
    quiet: true

  # noisy_chain:
  #   chain:
  #     - rule: noisy_message
  #     - rule: noisy_message2

  # quiet_chain:
  #   chain:
  #     - rule: noisy_message
  #     - rule: noisy_message2
  #   quiet: true
