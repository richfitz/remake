sources:
  quiet.R

targets:
  noisy_message:
    command: run_noisy_message()
  noisy_cat:
    command: run_noisy_cat()
  noisy_warning:
    command: run_noisy_warning()
  noisy_error:
    command: run_noisy_error()

  quiet_message:
    command: run_noisy_message()
    quiet: true
  quiet_cat:
    command: run_noisy_cat()
    quiet: true
  quiet_warning:
    command: run_noisy_warning()
    quiet: true
  quiet_error:
    command: run_noisy_error()
    quiet: true

  # noisy_chain:
  #   command:
  #     - run_noisy_message()
  #     - run_noisy_message2()

  # quiet_chain:
  #   command:
  #     - run_noisy_message()
  #     - run_noisy_message2()
  #   quiet: true
